// MarkdownCore — umbrella. Real types live in Parser/, RangeMapping/, etc.
// Placeholder so the target has a source file; replaced as Phase 1 fills in.

public enum MarkdownCore {
    public static let version = "0.1.0"
}
